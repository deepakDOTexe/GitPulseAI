# Core dependencies for Streamlit Cloud deployment
streamlit>=1.25.0

# Google Gemini LLM
google-generativeai>=0.3.0

# Supabase for vector database (Option 3)
supabase>=2.3.0

# OpenAI for embeddings (when using Supabase vector store)
openai>=1.6.0

# Scientific computing (optimized versions)
numpy>=1.24.0
scikit-learn>=1.3.0

# Data processing
pandas>=2.0.0

# Environment management  
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Logging
loguru>=0.7.0

# Optional: Web scraping (only for local development)
# beautifulsoup4>=4.12.0
# lxml>=6.0.0

# Optional: Local embeddings (only for precomputation, not cloud deployment)
# sentence-transformers>=2.2.0 